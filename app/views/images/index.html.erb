<div class="card" style="width:95%;margin:0 auto;margin-top:20px;padding:20px">

<p id="notice"><%= notice %></p>

<h1>Listing Images</h1>

<table class="highlight">
  <thead>
    <tr>
      <th>Filename</th>
      <th>Title</th>
      <th>Date created</th>
      <th>Image Preview</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @images.each do |image| %>
      <tr>
        <td><%= image.filename %></td>
        <td><%= image.title %></td>
        <td><%= image.dateCreated %></td>
        <td>    <% image.photos.each do |photo| %>
                <%= cl_image_tag(photo.path, { size: '100x100', crop: :fit }) %>
            <% end %>
          </td>
        <td><%= link_to 'Show', image %></td>
        <td><%= link_to 'Edit', edit_image_path(image) %></td>
        <td><%= link_to 'Delete', image, method: :delete, data: { confirm: 'Are you sure? This action cannot be undone' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<hr>
<br>

</div>
